# rate-your-neighbor
Application de reviews de voisinage avec attribution de récompenses / pénalitées

## Lancement rapide avec Docker Compose

```bash
docker-compose up -d
```

- Accédez au frontend sur http://localhost:3000
- L’API backend sur http://localhost:5000/api
- phpMyAdmin sur http://localhost:8080
